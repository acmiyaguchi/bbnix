# bbnix — path to your BlackBerry 10 Native SDK tree (the gitignored, never
# committed bbndk-linux). Required: there is no default, and builds read it in
# place (Model A), so package builds run with --impure, e.g.:
#   nix build --impure .#gcc
BBNIX_SYSROOT=/path/to/bbndk-linux
